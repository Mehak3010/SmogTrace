import React from 'react';
import ReactDOM from 'react-dom/client';
import StubbleDashboard from './StubbleDashboard.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StubbleDashboard />
  </React.StrictMode>
);
